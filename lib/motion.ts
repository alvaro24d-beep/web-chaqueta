/**
 * Token único de easing de la web (antes copiado a mano en 7 archivos).
 * Es un ease-out fuerte; en CSS existe como --ease-out (globals.css).
 */
export const EASE_OUT: [number, number, number, number] = [0.16, 1, 0.3, 1];
